{% extends 'base.html.twig' %}

{% block title %}RolesUsuario{% endblock %}

{% block body %}
    <h1>RolesUsuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roles_usuario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ roles_usuario.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ roles_usuario.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roles_usuario_index') }}">back to list</a>

    <a href="{{ path('app_roles_usuario_edit', {'id': roles_usuario.id}) }}">edit</a>

    {{ include('roles_usuario/_delete_form.html.twig') }}
{% endblock %}
