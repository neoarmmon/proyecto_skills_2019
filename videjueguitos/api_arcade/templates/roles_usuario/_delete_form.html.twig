<form method="post" action="{{ path('app_roles_usuario_delete', {'id': roles_usuario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ roles_usuario.id) }}">
    <button class="btn">Delete</button>
</form>
